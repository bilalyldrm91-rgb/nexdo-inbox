name: Email Check

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  check-email:
    runs-on: ubuntu-latest
    steps:
      - name: Check all active email channels
        run: |
          curl -s -X GET "${{ secrets.VERCEL_URL }}/api/check-email?days=30"
